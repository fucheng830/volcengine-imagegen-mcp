# 🚀 测试执行指南

## 📋 测试准备检查清单

### ✅ 步骤 1: Python 环境检查（已完成）

**状态**: ✅ 已完成
- Python 版本: 3.14.2
- fastmcp: 已安装（版本 2.14.3）
- httpx: 已安装（版本 0.28.1）
- pydantic: 已安装（版本 2.12.4）
- python-dotenv: 已安装（版本 1.1.0）

---

### ✅ 步骤 2: 配置 ARK_API_KEY（需要你完成）

**状态**: ⚠️ 需要你完成配置

#### 方法 A: 使用 .env 文件（推荐）

1. **获取火山引擎 API Key**:
   - 访问火山引擎 ARK 控制台：https://console.volcengine.com/ark
   - 登录你的账号
   - 在左侧菜单选择 "API Key 管理"
   - 点击 "创建 API Key"
   - 复制生成的 API Key

2. **配置 .env 文件**:
   - 打开文件：`d:\程序\workspace\opensource\volcengine-imagegen-mcp\.env`
   - 将 `your_api_key_here` 替换为你的实际 API Key
   - 保存文件

   例如：
   ```bash
   ARK_API_KEY=abcd1234-5678-90ef-ghij-klmnopqrstuv
   ```

3. **验证配置**:
   ```powershell
   # 在 PowerShell 中运行
   cd "d:\程序\workspace\opensource\volcengine-imagegen-mcp"
   python -c "from dotenv import load_dotenv; load_dotenv(); import os; print('API Key 设置成功!' if os.getenv('ARK_API_KEY') else 'API Key 未设置')"
   ```

#### 方法 B: 设置系统环境变量

**Windows PowerShell（临时）**:
```powershell
$env:ARK_API_KEY="your_api_key_here"
```

**Windows CMD（临时）**:
```cmd
set ARK_API_KEY=your_api_key_here
```

**Windows 系统环境变量（永久）**:
1. 打开 "系统属性" → "高级" → "环境变量"
2. 在"用户变量"中点击"新建"
3. 变量名：`ARK_API_KEY`
4. 变量值：你的 API Key
5. 点击"确定"保存
6. 重启 PowerShell 或 VS Code

---

### ✅ 步骤 3: 测试 MCP Server（需要你完成）

**状态**: ⏳ 等待 API Key 配置完成后执行

#### 启动 Python MCP Server

```powershell
# 进入项目目录
cd "d:\程序\workspace\opensource\volcengine-imagegen-mcp"

# 启动 MCP Server
python server.py
```

**预期输出**:
```
火山引擎 AI 生图 MCP 服务器已启动！
按 Ctrl+C 停止服务器
```

---

## 🧪 测试执行步骤

### 测试环境准备

**选项 1: Claude Desktop（推荐用于测试）**

1. **配置 Claude Desktop**:
   - 打开 Claude Desktop 应用
   - 打开设置 → Developer → Edit Config
   - 添加以下配置：

   ```json
   {
     "mcpServers": {
       "volcengine-imagegen": {
         "command": "python",
         "args": [
           "d:\\程序\\workspace\\opensource\\volcengine-imagegen-mcp\\server.py"
         ],
         "env": {
           "ARK_API_KEY": "your_api_key_here"
         }
       }
     }
   }
   ```

2. **重启 Claude Desktop**:
   - 完全关闭 Claude Desktop
   - 重新打开应用
   - 等待 MCP Server 连接（约 10 秒）

3. **验证连接**:
   - 在 Claude 对话框输入：`列出所有支持的模型`
   - 应该看到模型列表返回

**选项 2: VS Code（使用 Cline）**

1. **安装 Cline 扩展**:
   - 在 VS Code 中搜索并安装 "Cline" 扩展

2. **配置 MCP Server**:
   - 打开 VS Code 设置（Ctrl + ,）
   - 搜索 "cline mcp"
   - 添加 MCP Server 配置：

   ```json
   {
     "cline.mcpServers": {
       "volcengine-imagegen": {
         "command": "python",
         "args": [
           "d:\\程序\\workspace\\opensource\\volcengine-imagegen-mcp\\server.py"
         ],
         "env": {
           "ARK_API_KEY": "your_api_key_here"
         }
       }
     }
   }
   ```

3. **重启 VS Code**

---

## 📝 测试用例执行

### TC001: 简单文生图测试

**测试步骤**:
1. 在 Claude Desktop 中输入：
   ```
   生成一张简单的风景画，描绘宁静的湖泊和远山
   ```

2. 等待响应（预计 30-60 秒）

3. 验证结果：
   - ✅ 返回成功消息
   - ✅ 包含图片下载链接
   - ✅ 显示模型、提示词、尺寸信息
   - ✅ 响应时间 < 60 秒

**预期输出示例**:
```
✅ 成功生成 1 张图片！

📊 生成信息：
- 模型: doubao-seedream-4.5
- 提示词: 生成一张简单的风景画，描绘宁静的湖泊和远山
- 尺寸: 2K
- 文本权重: 2.5
- 水印: 否

🖼️ 图片链接：
https://ark-cn-beijing.volces.com/images/xxx.jpg

⏱️ 响应时间: 35 秒
```

---

### TC002: 完整参数测试

**测试步骤**:
1. 在 Claude Desktop 中输入：
   ```
   生成一张专业摄影师拍摄的商业肖像，自然光，眼神自信，背景简洁，8K分辨率，文本权重 7.5，添加水印
   ```

2. 验证所有参数是否正确传递：
   - model: doubao-seedream-4.5
   - size: 4K
   - guidance_scale: 7.5
   - watermark: true

---

### TC003: Base64 格式测试

**测试步骤**:
1. 在 Claude Desktop 中输入：
   ```
   生成一张简约风格的企业 Logo，以 Base64 格式返回
   ```

2. 验证结果：
   - ✅ Base64 格式正常工作
   - ✅ 图片可以在 Claude 中直接显示
   - ✅ 无需下载步骤

---

### TC004: 多张文生图测试

**测试步骤**:
1. 在 Claude Desktop 中输入：
   ```
   生成 3 张不同风格的城市夜景，包括霓虹灯、高楼、赛博朋克风格
   ```

2. 验证结果：
   - ✅ 批量生成功能正常
   - ✅ 正确生成 3 张图片
   - ✅ 所有图片链接都有效

---

### TC007: 模型列表查询测试

**测试步骤**:
1. 在 Claude Desktop 中输入：
   ```
   列出所有支持的模型和它们的功能
   ```

2. 验证结果：
   - ✅ 显示所有 4 个支持的模型
   - ✅ 包含每个模型的功能说明
   - ✅ 显示支持的尺寸列表

**预期输出示例**:
```
🎨 支持的模型：

1. doubao-seedream-4.5 - 豆包生图 4.5 (最新，支持多格式)
   - 支持功能: 文生图, 图生图, 组图生成, 多图参考
   - 支持格式: PNG, JPG, WEBP, BMP, TIFF, GIF

2. doubao-seedream-4.0 - 豆包生图 4.0
   - 支持功能: 文生图, 图生图, 组图生成, 多图参考

3. doubao-seedream-3.0-t2i - 豆包生图 3.0 文生图
   - 支持功能: 文生图

4. doubao-seedream-3.0-i2i - 豆包生图 3.0 图生图
   - 支持功能: 图生图

📏 支持的尺寸: 2K, 4K, 1024x1024, 2048x2048
```

---

### TC008: API Key 错误处理测试

**测试步骤**:
1. **临时移除 API Key**:
   ```powershell
   # 在 PowerShell 中临时移除
   $env:ARK_API_KEY = ""
   ```

2. **重启 Claude Desktop**

3. **在 Claude 中输入测试请求**:
   ```
   生成一张风景画
   ```

4. **验证错误处理**:
   - ✅ 显示错误消息
   - ✅ 提示用户设置 API Key
   - ✅ 提供配置指导

**预期错误消息**:
```
❌ 错误：未设置 ARK_API_KEY 环境变量。

请按以下步骤设置火山引擎 API Key：

1. 访问火山引擎 ARK 控制台获取 API Key
2. 配置 .env 文件或设置系统环境变量
3. 重启 Claude Desktop

详细配置请参考: CLAUDE_GUIDE.md
```

---

## 📊 测试记录模板

### 测试记录表格

| 测试编号 | 测试名称 | 状态 | 响应时间 | 备注 |
|-----------|----------|------|---------|------|
| TC001 | 简单文生图 | ⬜ | ___ 秒 | |
| TC002 | 完整参数测试 | ⬜ | ___ 秒 | |
| TC003 | Base64 格式测试 | ⬜ | ___ 秒 | |
| TC004 | 多张文生图测试 | ⬜ | ___ 秒 | |
| TC007 | 模型列表查询 | ⬜ | ___ 秒 | |
| TC008 | API Key 错误处理 | ⬜ | ___ 秒 | |

### 单个测试详细记录

```
测试编号: TC001
测试时间: 2026-01-21 HH:MM
测试环境: Claude Desktop

测试输入:
  提示词: 生成一张简单的风景画，描绘宁静的湖泊和远山

测试结果:
  状态: ⬜ 通过 / ❌ 失败
  成功: 是/否
  响应时间: ___ 秒
  图片数量: __ 张
  备注: ___________

问题:
  无

改进建议:
  无
```

---

## 🔍 常见问题排查

### 问题 1: MCP Server 无法启动

**症状**:
- Claude Desktop 提示 "Failed to connect to MCP server"
- 服务器日志显示错误

**解决方法**:
1. 确认 Python 环境正确：`python --version`
2. 确认依赖包已安装：`pip show fastmcp`
3. 检查 server.py 文件路径是否正确
4. 查看 Claude Desktop 日志：Help → Developer → Toggle Log

---

### 问题 2: API Key 相关错误

**症状**:
- 错误消息："未设置 ARK_API_KEY 环境变量"
- API 返回 401 Unauthorized

**解决方法**:
1. 确认 API Key 已正确设置
2. 检查 API Key 是否有效（在火山引擎控制台验证）
3. 确认 API Key 未过期
4. 重启 Claude Desktop 使环境变量生效

---

### 问题 3: 图片生成失败

**症状**:
- 错误消息："图片生成失败"
- API 返回 4xx 或 5xx 错误

**解决方法**:
1. 检查网络连接
2. 确认 API 账户余额充足
3. 查看具体错误消息
4. 尝试使用简单的提示词测试

---

### 问题 4: 响应时间过长

**症状**:
- 单张图片生成超过 120 秒
- Claude 提示超时

**解决方法**:
1. 检查网络速度
2. 尝试使用较小的尺寸（2K 而不是 4K）
3. 减少批量生成的数量
4. 检查火山引擎 API 服务状态

---

## 🎉 测试完成后的步骤

### 1. 生成测试报告

完成所有测试后，根据测试记录生成总结报告，参考 `TEST_PLAN.md` 中的"测试总结报告模板"。

### 2. 反馈问题

如果发现任何问题，请记录：
- 问题描述
- 复现步骤
- 错误消息
- 环境信息

### 3. 验证功能

确认以下功能都正常工作：
- ✅ 基本文生图
- ✅ 完整参数处理
- ✅ Base64 格式支持
- ✅ 批量生成
- ✅ 模型列表查询
- ✅ 错误处理

---

## 📞 获取帮助

如果在测试过程中遇到问题：

1. **查看文档**:
   - README.md - 项目主文档
   - CLAUDE_GUIDE.md - Claude Desktop 配置指南
   - VS_CODE_GUIDE.md - VS Code 配置指南
   - TEST_PLAN.md - 测试计划

2. **检查日志**:
   - Claude Desktop 日志：Help → Developer → Toggle Log
   - MCP Server 日志：查看服务器控制台输出

3. **验证配置**:
   - Python 环境：`python --version`
   - 依赖包：`pip list`
   - API Key：`echo $env:ARK_API_KEY`

---

**准备好开始测试了吗？** 🚀

请按照以下顺序执行：

1. ✅ 配置 ARK_API_KEY（步骤 2）
2. ✅ 启动 MCP Server（步骤 3）
3. ✅ 在 Claude Desktop 中执行测试用例（TC001-TC008）
4. ✅ 记录测试结果
5. ✅ 生成测试报告

祝你测试顺利！🎉
